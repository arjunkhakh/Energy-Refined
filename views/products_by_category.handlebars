<h1>{{ category }} Products</h1>

{{#each products as |product| }}
<p>{{product.name}}</p>
<div class="container">
  <div class="row">
    <div class="col">
      {{product.name}}
    </div>
    <div class="col">
      {{product.description}}
    </div>
  </div>
</div>
{{/each}}
